server:
  port: 8006 #配置应用端口
spring:
  application:
    name: hystrix-dashboard #配置服务的名称
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: 1.117.215.215:8848  #  注册中心地址
hystrix:
  dashboard:
    proxy-stream-allow-list: "localhost"